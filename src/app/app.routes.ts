import { Routes } from '@angular/router';
import { PageLoginComponent } from './public/pages/login/page-login.component';

export const routes: Routes = [
  { path: 'login', component: PageLoginComponent },
];
